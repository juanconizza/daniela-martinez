---
import MainLayout from '../../layouts/MainLayout.astro'
import Vsl from '../../sections/metodo-rec/Vsl.astro'

const vimeoUrl = import.meta.env.PUBLIC_METODO_REC_VIMEO_URL ?? ''
---

<MainLayout
	title="Método REC | Así dejé de sufrir por amor y recuperé mi paz"
	description="Descubrí cómo dejar de sufrir por amor y recuperar tu paz en menos de 90 días. Mirá el video completo y agendá tu cita gratuita."
	keywords="dejar de sufrir por amor, recuperar la paz, apego, relaciones tóxicas, coaching para mujeres"
>
	<Fragment slot="head">
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<!-- Carga el CSS de Playfair Display sin bloquear el render: se pide con
		media="print" (no aplica de entrada) y el onload lo pasa a "all" apenas
		llega — mismo patrón que recomienda PageSpeed para hojas de estilo de
		terceros. El <noscript> es el fallback si JS está desactivado. -->
		<link
			href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,600;0,700;1,600&display=swap"
			rel="stylesheet"
			media="print"
			onload="this.media='all'"
		/>
		<noscript>
			<link
				href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,600;0,700;1,600&display=swap"
				rel="stylesheet"
			/>
		</noscript>

		<!-- El widget de Calendly (+ Stripe/reCAPTCHA que trae) ya NO se precarga
		acá — pesaba ~2.6MB de fondo para el 100% de las visitas, la mayoría
		sin llegar a agendar. Estos hints son gratis (no bajan contenido, solo
		DNS/TLS) y siguen agilizando el click real a /metodo-rec/agendar. -->
		<link rel="preconnect" href="https://calendly.com" />
		<link rel="preconnect" href="https://assets.calendly.com" />
		<link rel="prefetch" href="/metodo-rec/agendar" />
	</Fragment>

	<Vsl vimeoUrl={vimeoUrl} spotsLeft={4} />
</MainLayout>
